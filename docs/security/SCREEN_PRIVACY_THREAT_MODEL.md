# Screen privacy threat model

Threats include personal identifiers in HUD/chat, accidental raw-frame persistence, untrusted OCR prompt injection, and public overlay leakage. Controls are bounded still capture, in-memory redaction, untrusted-text labels, expiring observations, private operator evidence views, and no raw screenshot logging. Capture is disabled by default unless explicitly enabled.
