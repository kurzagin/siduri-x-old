# OBS integration

Future OBS WebSocket v5 integration authenticates locally, discovers configured scenes/sources, requests screenshots, reports stream/recording state, and toggles the overlay. It must not assume fixed scene names and should preserve a local kill switch.
