# VOICEVOX integration

The future headless flow is health check → discover speaker/style metadata → identify Nurse Robo Type T by metadata → audio query → synthesis → queue/playback → amplitude events. No permanent numeric style ID is stored. Attribution and applicable character terms must be completed before public streaming.
