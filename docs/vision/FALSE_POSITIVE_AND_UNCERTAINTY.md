# False positives and uncertainty

The system prefers unresolved output to a confident wrong label. Exact matches below the confidence threshold, ambiguous matches, conflicting temporal readings, stale observations, and failed vision passes must remain uncertain. Evaluation records false positives separately from missed detections.
