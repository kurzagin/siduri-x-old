# Vision evaluation set

The evaluation set should contain clear, ambiguous, occluded, stale, conflicting, and redacted fixtures for party HUD, active-character indicators, OCR, and named entities. Each case defines expected readings, acceptable alternatives, confidence bounds, and forbidden hidden-state inferences. No live account screenshots belong in Git.
