# Vision pipeline

OBS is the capture boundary. Future processing will skip near-duplicate frames, redact sensitive regions, identify the game, OCR useful crops, call a configured vision capability, and publish expiring evidence-backed observations. Raw full-screen persistence is disabled by default.
