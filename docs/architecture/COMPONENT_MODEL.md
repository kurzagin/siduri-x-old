# Component model

Components are: orchestrator; model router; persona/policy; memory; vision; voice; OBS; platform adapters; knowledge sources; storage; telemetry; overlay; operator console. Only the orchestrator publishes response plans. The public overlay has no operator controls.
