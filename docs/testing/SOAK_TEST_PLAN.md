# Soak test plan

Run the orchestrator and static clients for a representative stream session with synthetic platform events, bounded OBS captures, reconnects, and speech queue activity. Monitor memory, event counts, SQLite growth, socket reconnects, duplicate suppression, and audit continuity. Stop on raw-frame persistence, private-data leakage, unbounded queue growth, or unauthorized outbound action.
