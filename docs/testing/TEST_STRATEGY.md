# Test strategy

Unit tests cover contracts, capability failures, fake knowledge, repository behavior, and HTTP endpoints. Browser checks should later verify WebSocket reconnect, response rendering, and separation of public/private controls. Translation checks should compare entity/number/uncertainty/action extraction from the three renderings. CI needs no cloud credentials.
