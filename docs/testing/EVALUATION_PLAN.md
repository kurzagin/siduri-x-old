# Evaluation plan

Run offline contract tests first, then fixture evaluation, then operator-approved provider tests. Measure recipient correctness, private-memory exclusion, uncertainty preservation, language alignment, event deduplication, approval enforcement, and degraded-mode behavior. Live tests must use disposable test channels and synthetic messages.
