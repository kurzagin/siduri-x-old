# Data retention

Observations and platform event inbox entries are bounded and short-lived. Raw frames are never persisted. Memory and outbound audit records persist only as needed for approved operation and review. Delete expired, rejected, and unnecessary platform data according to operator policy; retain provenance for approved canonical memory and sent actions.
