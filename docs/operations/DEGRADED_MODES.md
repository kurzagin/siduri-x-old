# Degraded modes

If model routing fails, use cautious bounded text. If vision fails or expires, remain uncertain. If VOICEVOX fails, show subtitles only. If OBS disconnects, stop capture safely. If a platform fails, keep the local overlay loop running and disable that platform's sender. No degraded mode may send publicly without approval.
